# API base URL used by the frontend.
# Leave blank in dev — Vite's proxy forwards /api to http://localhost:4000.
# In production (Vercel), the rewrite in vercel.json forwards /api to the Render backend.
VITE_API_BASE_URL=
